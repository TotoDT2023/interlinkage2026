# IMM Action Wheel Preview

Temporary GitHub Pages preview package.

Open `index.html` locally or publish the repository with GitHub Pages from the repository root.

Access code:

```text
20IMM26
```

This package contains only the generated static dashboard preview. It does not include the source DOCX files or raw research working files.
